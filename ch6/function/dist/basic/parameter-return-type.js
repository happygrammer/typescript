/**
 * @author happygrammer
 */
function max(x, y) {
    if (x > y) {
        return x;
    }
    else {
        return y;
    }
}
var a = max(1, 2);
// let b = max("abc", "ABC");
console.log("a=" + a);
